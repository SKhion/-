# create

`business/scripts/*.py` が生成する成果物（企業分析・診断・提案書・見積・請求書・SNSカレンダー等）の出力先。
クライアント固有の生成物のため `.gitignore` でリポジトリには含めていない（テンプレート自体は
`business/proposal` `business/estimate` `business/branding` 等の各ディレクトリを参照）。
