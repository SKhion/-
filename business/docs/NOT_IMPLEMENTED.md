# 未実装一覧（正直な棚卸し）

実運用で「営業開始・販売・納品・自動更新」に踏み込むには、以下の外部接続・実データ投入が必要。
コード/設計自体は用意済みだが、鍵・契約・実データが無いと動かない部分を明記する。

## 外部サービス接続（鍵・契約が必要）
- [ ] ANTHROPIC_API_KEY の実発行・設定（現状は全スクリプトがモックモードで動作確認のみ）
- [ ] Stripe 決済・請求連携（`business/api` にエンドポイント未実装、Webhook受信も未実装）
- [ ] Gmail/LINE/Slack の実通知連携（`.env.example` に変数は用意、実装コードは未接続）
- [ ] n8nインスタンスの構築とワークフローのActivate（雛形JSONのみ用意、実運用未接続）
- [ ] Vercel等へのLP/ダッシュボードの実デプロイ（ローカル動作確認のみ）
- [ ] Supabaseへの本番移行（`crm/schema.sql` は用意済み、実際のプロジェクト作成・移行は未実施）

## 実データ・実業務
- [ ] 実在する見込み客リストの投入（CRMは空の状態、seedはデモデータのみ）
- [ ] 実際の会社名・連絡先・特定商取引法表記（LPはテンプレート値のまま）
- [ ] 契約書/NDAの弁護士レビュー（テンプレートに法的助言なしと明記済み）
- [ ] 請求書のインボイス制度対応項目（登録番号等）の追記

## 機能として未着手
- [ ] 電子契約サービス（Cloudsign等）との連携
- [ ] 画像生成AI（Gemini等）を実際に呼び出す処理（現状はプロンプトを生成するのみ）
- [ ] チャットボット（LINE/Web、採点表で不採用としたが将来検討の余地あり）
- [ ] 認証・権限管理（現状のAPIは社内限定利用前提でオープン、本番公開前に認証必須）

これらは `docs/NEXT_IMPROVEMENTS.md` と `docs/TOMORROW_MORNING.md` に優先順位付きで反映済み。
